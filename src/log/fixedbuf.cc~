//

#include "fixedbuf.h"

#inclde <string>

#include "string.h"

using namespace std;
using namespace summer;
using namespace summer::log;

int FixedBuffer::append(char* str, int len)
{
  if ( avail() < len ){
    memcpy(cur_,str,len);}
  return len;
}
